apiVersion: v1
kind: Service
metadata:
  name: {{ include "WeatherForecastTest-api.fullname" . }}
  labels:
    {{- include "WeatherForecastTest-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "WeatherForecastTest-api.selectorLabels" . | nindent 4 }}
